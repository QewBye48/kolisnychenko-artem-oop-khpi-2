# Відповідність вимогам ЛР14

- 3 класи предметної області: `Movie`, `Podcast`, `MediaCatalog`
- незалежні завдання: сканування каталогу, сортування фільмів, генерація рекомендацій
- потік через `extends Thread`: `CatalogScanThread`
- завдання через `implements Runnable`: `SortTask`, `RecommendationTask`
- запуск через `start()`: так
- очікування завершення через `join()`: так
- використання `sleep()`: так
- виведення імен потоків: так
- демонстраційна програма `Main`: так
- модульні тести для детермінованої логіки: так